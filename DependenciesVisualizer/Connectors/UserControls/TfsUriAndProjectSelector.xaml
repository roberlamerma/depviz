<UserControl x:Class="DependenciesVisualizer.Connectors.UserControls.TfsUriAndProjectSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DependenciesVisualizer.Connectors.UserControls"
             mc:Ignorable="d" >
    <Grid Width="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" 
               HorizontalAlignment="Center"
               Content="Connect to a Server/Project" />
        <TextBox Grid.Row="1" 
            Margin="5, 2, 5, 0"
            Text="{Binding Path=TfsUri, TargetNullValue='[TfsUri]'}" />
        <TextBox Grid.Row="2"
            Margin="5, 2, 5, 0"
            Text="{Binding Path=ProjectName, TargetNullValue='[Project]'}" />
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal" FlowDirection="RightToLeft">
            <Button 
                Margin="5, 5, 5, 0"
                Content="Cancel" Width="80" />
            <Button 
                Margin="0, 5, 5, 0"
                Content="Connect" Width="80" />
        </StackPanel>
    </Grid>
</UserControl>

