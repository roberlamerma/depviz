<UserControl x:Class="DependenciesVisualizer.Connectors.UserControls.CsvConnector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0"
            Content="Selected CSV:" FontWeight="Bold" />
        <TextBox Grid.Row="1" Grid.Column="0"
                 Margin="5, 0, 5, 0"
                 Text="{Binding Path=SelectedCsvFile, TargetNullValue='[No CSV selected]'}" IsReadOnly="True" />
        <Button Grid.Row="1" Grid.Column="1"
                Margin="0, 0, 5, 0"
                Content="Select CSV..." Command="{Binding Path=PickCsvFile}"
            />
    </Grid>
</UserControl>
